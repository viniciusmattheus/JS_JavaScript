function media(nota1, nota2){
    return (nota1+nota2)/2;
}
var num=media(8, 7);
document.write("Num = "+num);

//A palavra reservada return é utilizada para definir o resultado da função e sinaliza também o fim da execução desta.
//Qualquer códiugo que venha a ser posto após o return (o que não deve ocorrer) será desconsiderado.